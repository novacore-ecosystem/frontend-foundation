# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Project status

This repository (`frontend-foundation`) is a fresh scaffold with no source code yet — only a stub `package.json` (pnpm-managed, no dependencies, no real scripts) and no commits on `main`. There is no established architecture, build process, or test setup to document.

Update this file with real commands and architecture notes once the project takes shape.
